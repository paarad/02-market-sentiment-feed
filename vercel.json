{
  "buildCommand": "cd web && npm run build",
  "outputDirectory": "web/.next",
  "installCommand": "cd web && i=0; until [ \"$i\" -ge 5 ]; do (npm ci --no-audit --no-fund --prefer-offline || npm install --no-audit --no-fund --prefer-offline) && break; i=$((i+1)); echo \"install failed, retry $i/5 in 5s...\"; sleep 5; done",
  "framework": "nextjs"
} 